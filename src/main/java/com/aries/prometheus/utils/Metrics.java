package com.aries.prometheus.utils;

public class Metrics {
    public enum metrics{
        service_time,
        service_rate,
        socket_count,
        gc_count,
        gc_time,
        heap_committed,
        heap_used,
        heap_usage,
        nonheap_used
    }
}
